name: Task 1 corrected latent usage audit

on:
  workflow_dispatch:

jobs:
  audit:
    runs-on: ubuntu-24.04

    env:
      OPENBLAS_NUM_THREADS: "1"
      OMP_NUM_THREADS: "2"

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Python 3.12
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Install research environment
        run: |
          python -m pip install --upgrade pip
          python -m pip install -r requirements-research.txt

      - name: Show platform and package versions
        run: |
          python - <<'PY'
          import platform
          import torch, numpy, pandas, scanpy, anndata, sklearn
          print("Platform:", platform.platform())
          print("Machine:", platform.machine())
          print("Python:", platform.python_version())
          print("Torch:", torch.__version__)
          print("NumPy:", numpy.__version__)
          print("Pandas:", pandas.__version__)
          print("Scanpy:", scanpy.__version__)
          print("AnnData:", anndata.__version__)
          print("scikit-learn:", sklearn.__version__)
          PY

      - name: Download public inputs
        run: python scripts/05_download_research_data.py

      - name: Recover corrected cohort
        run: python scripts/05_recover_cohort.py

      - name: Verify recovered data hash
        run: |
          ACTUAL=$(sha256sum data/processed/recovered_counts.h5ad | awk '{print $1}')
          EXPECTED="62a5047e74d4ea704f2a10b5b91b35a4836ccefc99186d4c72101db78cbd8ac1"
          echo "Actual:   $ACTUAL"
          echo "Expected: $EXPECTED"
          test "$ACTUAL" = "$EXPECTED"

      - name: Prepare benchmark split
        run: python scripts/06_validation.py --prepare

      - name: Rebuild benchmark with committed gene universe
        run: |
          cp runs/corrected_20260920/inputs.npz /tmp/prepared_inputs.npz
          git restore analysis/corrected_20260920/benchmark_gene_universe.csv

          python - <<'PY'
          import numpy as np
          import pandas as pd
          import anndata as ad
          import scanpy as sc

          run = "runs/corrected_20260920"
          genes = pd.read_csv(
              "analysis/corrected_20260920/benchmark_gene_universe.csv"
          )["gene"].astype(str).tolist()

          old = np.load("/tmp/prepared_inputs.npz")
          train = old["train"].copy()
          val = old["val"].copy()
          test = old["test"].copy()

          a = ad.read_h5ad("data/processed/recovered_counts.h5ad")
          obs = a.obs.copy()

          missing = [g for g in genes if g not in a.var_names]
          assert not missing, f"Missing committed genes: {missing}"

          counts = a[:, genes].X.toarray().astype("float32")
          library = np.asarray(a.X.sum(1)).ravel().astype("float32")
          np.savez_compressed(
              f"{run}/count_inputs.npz",
              C=counts,
              library=library,
          )

          sc.pp.normalize_total(a, target_sum=1e4)
          sc.pp.log1p(a)
          X = a[:, genes].X.toarray().astype("float32")

          d = obs.drug.map({
              "control": 0,
              "doxorubicin": 1,
              "cisplatin": 2,
          }).values.astype("int64")
          b = (obs.study == "GSE271055").values.astype("int64")

          np.savez_compressed(
              f"{run}/inputs.npz",
              X=X,
              d=d,
              b=b,
              train=train,
              val=val,
              test=test,
              genes=np.asarray(genes, dtype="U"),
          )

          print("X shape:", X.shape)
          print("Canonical genes:", len(genes))
          print("Train/val/test:", len(train), len(val), len(test))
          assert X.shape == (46857, 1500)
          assert len(genes) == 1500
          assert len(train) == 6000
          assert len(val) == 2881
          assert len(test) == 2881
          PY

      - name: Train canonical full models
        run: |
          for seed in 0 1 2; do
            echo "===== TRAINING SEED $seed ====="
            python scripts/06_validation.py --mode full --seed "$seed" --epochs 100
          done

      - name: Verify canonical metrics reproduce
        run: |
          python - <<'PY'
          import json
          import subprocess

          for seed in (0, 1, 2):
              path = f"runs/corrected_20260920/full_{seed}_metrics.json"
              with open(path) as f:
                  new = json.load(f)

              old = json.loads(
                  subprocess.check_output(
                      ["git", "show", f"HEAD:{path}"],
                      text=True,
                  )
              )

              print(f"Seed {seed}")
              print("  committed F_sh:", old["shared_fraction"])
              print("  new F_sh:      ", new["shared_fraction"])
              print("  delta:         ", new["shared_fraction"] - old["shared_fraction"])
              print("  committed MSE: ", old["test_mse"])
              print("  new MSE:       ", new["test_mse"])

              assert abs(new["shared_fraction"] - old["shared_fraction"]) < 1e-6
              assert abs(new["test_mse"] - old["test_mse"]) < 1e-6
          PY

      - name: Run latent usage diagnostic
        run: python scripts/14_latent_usage.py

      - name: Show Task 1 summary
        run: |
          echo "===== LATENT USAGE SUMMARY ====="
          cat analysis/corrected_20260920/latent_usage_summary.csv
          echo
          echo "===== ACTIVE-UNIT / KL THRESHOLDS ====="
          cat analysis/corrected_20260920/latent_usage_thresholds.json

      - name: Collect Task 1 outputs
        run: |
          mkdir -p task1_corrected_latent_usage

          cp analysis/corrected_20260920/latent_usage_per_dimension.csv task1_corrected_latent_usage/
          cp analysis/corrected_20260920/latent_usage_summary.csv task1_corrected_latent_usage/
          cp analysis/corrected_20260920/latent_usage_thresholds.json task1_corrected_latent_usage/
          cp analysis/corrected_20260920/benchmark_gene_universe.csv task1_corrected_latent_usage/
          cp runs/corrected_20260920/inputs.npz task1_corrected_latent_usage/

          for seed in 0 1 2; do
            cp "runs/corrected_20260920/full_$seed.pt" task1_corrected_latent_usage/
            cp "runs/corrected_20260920/full_$seed"_latents.npz task1_corrected_latent_usage/
            cp "runs/corrected_20260920/full_$seed"_metrics.json task1_corrected_latent_usage/
            cp "runs/corrected_20260920/full_$seed"_history.csv task1_corrected_latent_usage/
          done

      - name: Upload Task 1 outputs
        uses: actions/upload-artifact@v4
        with:
          name: task1-corrected-latent-usage
          path: task1_corrected_latent_usage/
          retention-days: 7
